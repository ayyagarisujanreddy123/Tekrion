# Security verification map

> Source candidate: Black Box 0.1.0
>
> Verified: 2026-07-27

This document maps every item in the execution plan's privacy and security checklist to the implementation boundary and regression evidence that enforce it. It is intended to make the security posture reviewable instead of relying on an unchecked claim.

This is not a third-party security audit, a sandbox guarantee, or a claim that captured evidence is harmless. The trust boundaries and vulnerability-reporting process remain defined in the [security policy](../SECURITY.md).

## Verification commands

The complete local source gate runs formatting, lint, strict typechecking, unit tests, and the packaged end-to-end path:

```bash
npm run check
```

The release candidate also runs the package smoke test, benchmark, two offline demo rehearsals, and dependency audit described in the [release checklist](release-checklist.md). Platform CI must be associated with the exact candidate commit before a public release claim is made.

## Control evidence

| Control                                                                                            | Enforced by                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | Regression evidence                                                                                                                                                                                                                                                                                                                                                                       |
| -------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Authorization, API-key, account-routing, and cookie headers cannot enter persisted header evidence | [`SafeHeadersSchema`](../packages/protocol/src/raw-exchange.ts) rejects forbidden names at the durable contract boundary. [`headersForPersistence`](../apps/daemon/src/proxy/headers.ts) removes authorization, `x-api-key`, `ChatGPT-Account-ID`, cookie, proxy-auth, `set-cookie`, hop-by-hop, and configured sensitive headers before creating a raw record. Storage migrations scrub historical `x-api-key` and ChatGPT account-identifier fields from active records.                                 | [`contracts.test.ts`](../packages/protocol/test/contracts.test.ts), [`proxy-policy.test.ts`](../apps/daemon/test/proxy-policy.test.ts), [`recorder-proxy.test.ts`](../apps/daemon/test/recorder-proxy.test.ts), and [`storage.test.ts`](../packages/storage/test/storage.test.ts) verify schema rejection, in-memory forwarding, persistence exclusion, routing, and migration scrubbing. |
| Known fake credentials are absent from the database, blob store, and daemon log                    | The proxy stores only safe headers; the daemon child receives an allowlisted environment; normal request handling has no request/body logger.                                                                                                                                                                                                                                                                                                                                                              | [`recorder-proxy.test.ts`](../apps/daemon/test/recorder-proxy.test.ts) scans SQLite and decoded blobs. [`cli-lifecycle.test.ts`](../test/e2e/cli-lifecycle.test.ts) scans the packaged database, every blob, and `daemon.log` for authorization, cookie, control-token, and excluded environment secrets.                                                                                 |
| Private data paths use restrictive permissions                                                     | [`private-files.ts`](../apps/daemon/src/lifecycle/private-files.ts) enforces `0700` directories and `0600` sensitive files, rejects symlinked sensitive paths, and reads control files through a verified descriptor. Storage initialization and archive writes use the same private-file boundary.                                                                                                                                                                                                        | [`lifecycle.test.ts`](../apps/daemon/test/lifecycle.test.ts), [`cli.test.ts`](../apps/cli/test/cli.test.ts), and [`storage.test.ts`](../packages/storage/test/storage.test.ts) verify modes for the home, token, lock, database, blobs, and exports on POSIX hosts, including symlink refusal. The CLI test verifies Windows mode limitations as a non-fatal warning.                     |
| Database health checks do not mutate or silently migrate evidence                                  | [`openBlackBoxStorage`](../packages/storage/src/storage.ts) supports an explicit query-only, file-must-exist path. [`doctor.ts`](../apps/cli/src/doctor.ts) uses it to verify migration checksums, schema compatibility, and SQLite integrity.                                                                                                                                                                                                                                                             | [`storage.test.ts`](../packages/storage/test/storage.test.ts) verifies query-only access and rejected writes. [`cli.test.ts`](../apps/cli/test/cli.test.ts) verifies a healthy database pass and corrupt-database failure.                                                                                                                                                                |
| Control/cockpit listeners are loopback-only and the proxy defaults to loopback                     | [`ControlServer`](../apps/daemon/src/lifecycle/control-server.ts) rejects non-loopback binding. [`resolveProxyConfiguration`](../apps/daemon/src/proxy/config.ts) defaults to `127.0.0.1` and requires an explicit opt-in for a non-loopback proxy.                                                                                                                                                                                                                                                        | [`lifecycle.test.ts`](../apps/daemon/test/lifecycle.test.ts) verifies control-plane refusal. [`proxy-policy.test.ts`](../apps/daemon/test/proxy-policy.test.ts) verifies loopback recognition, defaults, and explicit proxy opt-in.                                                                                                                                                       |
| A proxy cannot be configured to forward to itself                                                  | [`assertNoProxyLoop`](../apps/daemon/src/proxy/config.ts) compares effective ports and normalized loopback/listener hosts before startup. The CLI deliberately ignores `OPENAI_BASE_URL` as an upstream source.                                                                                                                                                                                                                                                                                            | [`proxy-policy.test.ts`](../apps/daemon/test/proxy-policy.test.ts) covers IPv4, IPv6, and `localhost` loops. [`cli.test.ts`](../apps/cli/test/cli.test.ts) verifies the separate `BLACKBOX_UPSTREAM_URL` rule.                                                                                                                                                                            |
| An incoming absolute-form request target cannot replace the configured upstream origin             | [`parseRequestTarget`](../apps/daemon/src/proxy/recorder-proxy.ts) parses the client target against a non-routable sentinel and carries only pathname/query data into the operator-selected upstream. Query/header names are accumulated through maps before safe record construction.                                                                                                                                                                                                                     | [`recorder-proxy.test.ts`](../apps/daemon/test/recorder-proxy.test.ts) sends an absolute-form target naming a second server and proves only the configured upstream receives it. It also covers repeated `__proto__` and `constructor` query names.                                                                                                                                       |
| The cockpit enforces CSP, origin/host checks, and inert evidence rendering                         | [`viewer-assets.ts`](../apps/daemon/src/viewer/viewer-assets.ts) sends a restrictive CSP and browser policy headers. [`control-server.ts`](../apps/daemon/src/lifecycle/control-server.ts) validates loopback Host and trusted Origin before serving UI or API data. [`http-response.ts`](../apps/daemon/src/query/http-response.ts) serves captured payloads as sandboxed `application/octet-stream` attachments. Viewer evidence is rendered through escaped React text rather than HTML injection APIs. | [`lifecycle.test.ts`](../apps/daemon/test/lifecycle.test.ts) verifies CSP and cross-origin rejection. [`evidence-query-api.test.ts`](../apps/daemon/test/evidence-query-api.test.ts) verifies inert malicious payload delivery. Viewer model/API tests verify the authenticated fragment bootstrap and evidence presentation path.                                                        |
| The local control token is random, private, and absent from normal output                          | [`control-token.ts`](../apps/daemon/src/lifecycle/control-token.ts) creates a 256-bit random base64url token once. Status records do not contain it, and browser bootstrap uses a URL fragment that is removed after initialization.                                                                                                                                                                                                                                                                       | [`lifecycle.test.ts`](../apps/daemon/test/lifecycle.test.ts) verifies token shape, idempotence, private mode, authenticated control, and status exclusion. [`cli.test.ts`](../apps/cli/test/cli.test.ts) and [`cli-lifecycle.test.ts`](../test/e2e/cli-lifecycle.test.ts) verify it is absent from CLI output, viewer HTML, database, blobs, and daemon logs.                             |
| Request logging cannot stringify raw headers or bodies outside the evidence/redaction path         | [`recorder-proxy.ts`](../apps/daemon/src/proxy/recorder-proxy.ts) forwards streams and writes bounded evidence without a request logger. [`daemon-worker.ts`](../apps/cli/src/daemon-worker.ts) emits only lifecycle/error summaries, and [`daemon-launcher.ts`](../apps/cli/src/daemon-launcher.ts) passes an allowlisted environment, rotates one private log backup, and rejects unsafe log targets.                                                                                                    | The packaged [`cli-lifecycle.test.ts`](../test/e2e/cli-lifecycle.test.ts) sends distinctive header and environment secrets and scans the resulting daemon log. [`daemon-launcher.test.ts`](../apps/cli/test/daemon-launcher.test.ts) verifies rotation, modes, and unsafe-target refusal. Proxy failure tests verify errors remain fail-open without dumping captured content.            |
| Recorded evidence is explicitly delimited as untrusted in optional-analysis prompts                | [`openai-report-provider.ts`](../apps/daemon/src/analysis/openai-report-provider.ts) wraps the minimized snapshot between `BEGIN_UNTRUSTED_EVIDENCE_SNAPSHOT_JSON` and `END_UNTRUSTED_EVIDENCE_SNAPSHOT_JSON`, requests strict structured output, disables provider storage, and bounds the response.                                                                                                                                                                                                      | [`openai-report-provider.test.ts`](../apps/daemon/test/openai-report-provider.test.ts) verifies the exact delimiter, `store: false`, structured schema, safe endpoint policy, and response bound.                                                                                                                                                                                         |
| External AI analysis is disabled by default and requires reviewed consent                          | A provider exists only when dedicated `BLACKBOX_ANALYSIS_API_KEY` and `BLACKBOX_ANALYSIS_MODEL` values are configured. Deterministic reports are the default. AI execution requires a local preflight, `consent: true`, and the fingerprint of the reviewed snapshot; changed evidence invalidates that consent.                                                                                                                                                                                           | [`evidence-query-api.test.ts`](../apps/daemon/test/evidence-query-api.test.ts), [`evidence-query-service.test.ts`](../apps/daemon/test/evidence-query-service.test.ts), and [`cli.test.ts`](../apps/cli/test/cli.test.ts) verify authentication, no-call preflight, explicit consent, stale-consent rejection, failure fallback, and `externalEvidenceSent` accounting.                   |
| `.bbx` paths and input sizes cannot escape their verification boundary                             | [`BbxArchiveEntryPathSchema`](../packages/protocol/src/archive.ts) permits only normalized relative paths and rejects roots, empty/`.`/`..` segments, backslashes, drive prefixes, and unsupported characters. [`readBbxArchiveFile`](../apps/daemon/src/archive/bbx-integrity.ts) reads through one descriptor with a hard byte ceiling; integrity verification requires payload paths to exactly match the manifest.                                                                                     | [`contracts.test.ts`](../packages/protocol/test/contracts.test.ts) covers absolute, drive, traversal, and ambiguous paths. [`bbx-integrity.test.ts`](../apps/daemon/test/bbx-integrity.test.ts) covers descriptor type and byte limits. [`bbx-archive.test.ts`](../apps/daemon/test/bbx-archive.test.ts) covers manifest/payload tampering and semantic import validation.                |
| Quota and retention safety is visible and plan-first                                               | [`blob-store.ts`](../packages/storage/src/blob-store.ts) refuses writes beyond `--max-stored-bytes` without partial metadata or silent eviction. `delete` and `prune` produce a plan and require `--yes`; active sessions and referenced blobs remain protected. `doctor` reports capture/storage bounds.                                                                                                                                                                                                  | [`storage.test.ts`](../packages/storage/test/storage.test.ts) covers capacity and disk-full atomicity. [`bbx-archive.test.ts`](../apps/daemon/test/bbx-archive.test.ts) covers active/linked evidence protection. [`cli.test.ts`](../apps/cli/test/cli.test.ts) verifies visible help, dry-run plans, explicit apply, and doctor limit reporting.                                         |
| The deterministic demo deletes only a clearly disposable target                                    | [`reset.mjs`](../demo/scripts/reset.mjs) refuses filesystem roots, the repository root, and any output basename that does not explicitly contain `blackbox-demo`; it creates a private, isolated copy before mutation.                                                                                                                                                                                                                                                                                     | [`rogue-demo.test.ts`](../packages/test-fixtures/test/rogue-demo.test.ts) verifies repeatable reset/cleanup, intact source fixtures, and refusal to remove an unsafe target containing a sentinel file. The packaged end-to-end test runs two clean offline rehearsals.                                                                                                                   |

## Residual risks

These controls reduce specific failure modes; they do not eliminate the product's inherent sensitivity:

- request bodies, source files, terminal output, and model text can contain secrets that header filtering cannot recognize;
- rule-based share redaction can miss novel credentials, personal data, or sensitive prose;
- anyone who can read the Black Box home may be able to read captured evidence;
- a forensic archive intentionally contains full retained evidence;
- the wrapped agent is not sandboxed and Black Box does not prevent its filesystem or network actions;
- archive hashes detect modification but are not signatures;
- loopback services still depend on host and browser security;
- no WebSocket/Realtime capture, Bedrock/Vertex normalization, or public custom
  adapter-event ingestion exists in 0.1.

Review exported artifacts before sharing them, keep the Black Box home private, and report suspected vulnerabilities through the private process in [`SECURITY.md`](../SECURITY.md).
